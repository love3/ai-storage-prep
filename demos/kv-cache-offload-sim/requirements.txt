# Core simulator (kvsim/ + cli.py) needs NOTHING beyond the standard library.
# The extras below are only for the optional FastAPI server and plotting.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
matplotlib>=3.7,<4.0   # optional: `python3 cli.py --plot out.png`
